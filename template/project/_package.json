{
  "name": "<%= projectName %>",
  "version": "0.0.1",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "dev": "miniapp build -w",
    "build": "miniapp build"
  },
  "author": "<%= gitName %>",
  "license": "ISC",
  "dependencies": {<% if (projectType == 'sass') { %>
    "node-sass": "^4.1.1",<% } %><% if (projectType == 'styl') { %>
    "stylus": "^0.54.5",<% } %>
    "babel-preset-es2015": "^6.18.0"
  }
}